# System / cache
.bundled_manifest
.curator_state
.usage.json
.usage.json.lock
.hub/
__pycache__/
*.pyc

# Temp
/tmp/
.curator_backups/

# Credenciais locais (NUNCA commit — chaves SSH/segredos)
**/references/*.pem
**/references/*.key
**/references/*_deploy_key*
