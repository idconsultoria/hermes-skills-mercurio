/* ============================================================
   Kindle Text Article CSS
   Optimized for Paperwhite 11th Gen (1236x1648, 300 PPI)
   Reflowable EPUB3 for long-form text reading
   ============================================================ */

@namespace epub "http://www.idpf.org/2007/ops";

body {
    font-family: Georgia, "Times New Roman", serif;
    line-height: 1.5;
    margin: 0;
    padding: 0;
    widows: 2;
    orphans: 2;
}

/* === Cover === */
.cover-page {
    text-align: center;
    padding-top: 20%;
}
.cover-title {
    font-size: 1.8em;
    font-weight: bold;
    line-height: 1.3;
    margin-bottom: 0.8em;
    text-indent: 0;
}
.cover-subtitle {
    font-size: 1.1em;
    font-style: italic;
    margin-bottom: 1.5em;
    text-indent: 0;
    color: #555;
}
.cover-authors {
    font-size: 0.9em;
    color: #777;
    margin-bottom: 1em;
    text-indent: 0;
}

/* === Chapter headings — always start on new page === */
h1.chapter-title {
    font-size: 1.5em;
    font-weight: bold;
    text-align: left;
    margin-top: 1.5em;
    margin-bottom: 0.8em;
    page-break-before: always;
    text-indent: 0;
    line-height: 1.3;
}

/* === Section headings === */
h2.section-title {
    font-size: 1.2em;
    font-weight: bold;
    margin-top: 1.2em;
    margin-bottom: 0.6em;
    page-break-after: avoid;
    text-indent: 0;
    line-height: 1.3;
}

/* === Body paragraphs — justified with indent === */
p {
    text-align: justify;
    text-indent: 1.2em;
    margin: 0.3em 0;
    line-height: 1.5;
    hyphens: auto;
    -webkit-hyphens: auto;
    widows: 2;
    orphans: 2;
}

p.no-indent {
    text-indent: 0;
}

/* === Figure captions === */
p.figure-caption {
    font-size: 0.85em;
    font-style: italic;
    text-align: center;
    text-indent: 0;
    margin: 0.8em 0;
    color: #555;
}

/* === Code blocks === */
pre, code {
    font-family: "Courier New", Courier, monospace;
    font-size: 0.85em;
}
pre {
    background: #f5f5f5;
    padding: 0.8em;
    margin: 0.8em 1em;
    white-space: pre-wrap;
    word-wrap: break-word;
    border-left: 2px solid #ddd;
    line-height: 1.4;
}
code {
    background: #f5f5f5;
    padding: 0.1em 0.3em;
    font-size: 0.9em;
}

/* === Blockquotes — research papers use these heavily === */
blockquote {
    margin: 0.8em 1.5em;
    font-style: italic;
    color: #555;
    border-left: 3px solid #ccc;
    padding-left: 1em;
}

/* === Tables === */
table {
    border-collapse: collapse;
    margin: 1em auto;
    font-size: 0.9em;
}
th, td {
    border: 1px solid #ccc;
    padding: 0.4em 0.6em;
    text-align: left;
}
th {
    background: #f0f0f0;
    font-weight: bold;
}

/* === Lists === */
ul, ol {
    margin: 0.5em 0;
    padding-left: 1.5em;
}
li {
    margin: 0.2em 0;
    line-height: 1.5;
}

/* === Separator === */
hr.separator {
    width: 30%;
    margin: 1.5em auto;
    border: none;
    border-top: 1px solid #ccc;
}

/* === Table of Contents === */
.toc-title {
    font-size: 1.5em;
    font-weight: bold;
    margin-bottom: 1em;
    text-indent: 0;
}
.toc-entry-chapter {
    font-weight: bold;
    margin: 0.6em 0 0.2em 0;
    text-indent: 0;
}
.toc-entry-section {
    padding-left: 1.5em;
    font-size: 0.9em;
    margin: 0.1em 0;
    text-indent: 0;
}
.toc-link {
    text-decoration: none;
    color: inherit;
}
