# Evolve Plan — 2026-08-09

## Estado inicial
- 102 skills, 0 orphans, 0 bad relation targets (verified via parser)
- 1 merge candidate identified

## Plano

### Merge 1: `messaging/whatsapp-automation` → `infrastructure/whatsapp-baileys-integration`
**Evidência:**
- Mesmo domínio: automação WhatsApp via Baileys, REST bridge Z-API-compatible
- Mesmo workflow: spawn Node bridge → QR auth → session persistence → send text/PDF
- `whatsapp-baileys-integration` é **superset estrito**: 20.6KB vs 5.7KB, cobre tudo do automation (architecture, QR, session, multi-assessor, Z-API migration) + 40 seções extras (pairing code 428, Cloud Run jobs, Drive/Sheets, ADC fallback, Gemini 429)
- Ambos `similar` no index.md (linhas 801, 1156)
- **Ação:**
  1. Copiar references únicos do automation (baileys-bridge-server.js, multi_assessor_config.py, whatsapp_client.py) para baileys-integration/references/
  2. Adicionar seção "Migration Checklist (Z-API → Baileys)" + "WhatsApp Web Limitations" + "Rate Limiting" ao SKILL.md do baileys-integration (conteúdo único do automation)
  3. `rm -rf messaging/whatsapp-automation/` (e dir messaging/ se vazio)
  4. index.md: remover entry do automation; atualizar relação `similar → messaging/whatsapp-automation` (linha 801) → apontar para outro alvo ou remover
  5. Atualizar grafo + log

### Manter separadas: selfhost-service-deploy × selfhost-web-apps
- Toolchain idêntica (docker compose + NPM + SSL na Oracle ARM64) mas workflows distintos:
  - service-deploy: deploy de QUALQUER serviço novo (padrão genérico de diretório, compose mínimo, pitfalls ARM64)
  - web-apps: deploy de apps web PHP/Python/Node com nginx frontend, hardening, SSL redirect loop PHP, permissões de config
- Já conectadas via `similar` bilateral — manter.

### Sem outras mudanças
- 0 orphans (todos 102 têm relações)
- Descrições todas conforme (audit 102/102 compliant)
- Sem deletes adicionais

## Resultado esperado
- 101 skills (102 − 1 merged)
- 0 orphans, 0 bad targets
- Grafo regenerado
